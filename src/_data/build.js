// Unique build identifier, regenerated on every Eleventy build.
// Used to cache-bust /css/style.css and /js/main.js so browsers always
// fetch the latest assets after a deploy.
export default String(Date.now());
