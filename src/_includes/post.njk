---
layout: base.njk
---
<article>
  <header class="post-header">
    <h1>{{ title }}</h1>
    {% if subtitle %}
      <p class="post-subtitle">{{ subtitle }}</p>
    {% endif %}
    {% if hashtags %}
      <p class="post-tags">{% for tag in hashtags %}#{{ tag }}{% if not loop.last %} {% endif %}{% endfor %}</p>
    {% endif %}
    <div class="post-meta">
      <time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time>
    </div>
  </header>
  <div class="post-content">
    {{ content | safe }}
  </div>
</article>
