---
layout: base.njk
title: Home
---

{% if collections.post.length > 0 %}
  <input
    class="post-filter post-filter-top"
    type="search"
    placeholder="Filter by title or #hashtag… (press / to focus)"
    aria-label="Filter posts"
  />
{% endif %}

<div class="home-grid">
  <div class="home-main">
    <h1>Posts</h1>
    <p class="home-intro">The writing portfolio of Kacper Ruta — essays, op-eds, and data-driven social commentary.</p>

    {% if collections.post.length == 0 %}
      <p>No posts yet.</p>
    {% else %}
      {% set lead = (collections.post | reverse) | first %}
      <a class="featured" href="{{ lead.url }}">
        {% if lead.data.thumbnail %}
          <img class="featured-thumb" src="{{ lead.data.thumbnail }}" alt="" loading="lazy" />
        {% endif %}
        <div class="featured-text">
          <p class="featured-kicker">Latest</p>
          <p class="featured-title">{{ lead.data.title }}</p>
          {% if lead.data.subtitle %}
            <p class="featured-sub">{{ lead.data.subtitle }}</p>
          {% endif %}
          <p class="featured-meta">
            {{ lead.date | readableDate }} · {{ lead.templateContent | readTime }}
          </p>
        </div>
      </a>

      <ul class="post-list">
        {% for post in collections.post | reverse %}
          <li data-search="{{ post.data.title }}{% for t in post.data.hashtags %} #{{ t }}{% endfor %}">
            {% if post.data.thumbnail %}
              <a class="post-list-thumb" href="{{ post.url }}">
                <img src="{{ post.data.thumbnail }}" alt="" loading="lazy" />
              </a>
            {% endif %}
            <div class="post-list-text">
              <a href="{{ post.url }}">{{ post.data.title }}</a>
              {% if post.data.subtitle %}
                <p class="post-list-subtitle">{{ post.data.subtitle }}</p>
              {% endif %}
              <p class="post-list-meta">
                <time datetime="{{ post.date | htmlDateString }}">
                  {{ post.date | readableDate }}
                </time>
                {% for t in post.data.hashtags %}<a class="tag-chip" href="/tags/{{ t | slugifyTag }}/">#{{ t }}</a>{% endfor %}
              </p>
            </div>
          </li>
        {% endfor %}
      </ul>
      <p class="post-filter-empty" hidden>No posts match.</p>
    {% endif %}
  </div>

  <aside class="home-feed" aria-label="Newsletter and latest posts on X">
    <div class="newsletter-box">
      <p class="home-feed-title">Newsletter</p>
      <p class="newsletter-blurb">Get new essays straight to your inbox.</p>
      <a class="newsletter-btn" href="{{ site.newsletter or ('mailto:' + site.email + '?subject=Subscribe%20me%20to%20the%20newsletter') }}">Subscribe</a>
    </div>

    <p class="home-feed-title">Latest on X</p>
    <a class="twitter-timeline"
       data-theme="light"
       data-chrome="noheader nofooter noborders transparent"
       data-tweet-limit="8"
       href="{{ site.social.twitter }}">
      Posts by @{{ site.twitterHandle }}
    </a>
  </aside>
</div>
